#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../raml-parser/bin/raml-parser" "$@"
  ret=$?
else 
  node  "$basedir/../raml-parser/bin/raml-parser" "$@"
  ret=$?
fi
exit $ret
