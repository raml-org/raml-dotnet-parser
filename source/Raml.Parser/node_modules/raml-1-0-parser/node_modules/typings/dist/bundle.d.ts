import Promise = require('native-or-bluebird');
import { CompiledOutput } from './lib/compile';
export interface Options {
    name?: string;
    source: string;
}
export declare function bundle(options: Options): Promise<CompiledOutput>;
