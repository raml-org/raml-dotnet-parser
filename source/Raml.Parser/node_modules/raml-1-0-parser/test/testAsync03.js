console.log('RAML 1.0 JS Parser Test');

var path = require("path");
var raml1Parser = require('../src/raml1Parser');

raml1Parser.loadApi("c:\desarrollo\mulesoft\raml-files\twitter.raml")
   .then( function(api){

    api.errors().forEach(function(x){
        console.log(JSON.stringify({
            code: x.code,
            message: x.message,
            path: x.path,
            start: x.start,
            end: x.end,
            isWarning: x.isWarning
            },null,2));
        });

        console.log(JSON.stringify(api.toJSON(), null, 2));
});
