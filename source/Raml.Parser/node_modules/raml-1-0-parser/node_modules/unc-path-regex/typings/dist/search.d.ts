import { SearchResults } from './lib/registry';
export interface Options {
    name?: string;
    source?: string;
    offset?: number | string;
    limit?: number | string;
}
export declare function search(query: string, options?: Options): Promise<SearchResults>;
